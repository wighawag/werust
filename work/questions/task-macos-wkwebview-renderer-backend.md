<!-- dorfl-sidecar: item=task:macos-wkwebview-renderer-backend type=task slug=macos-wkwebview-renderer-backend allAnswered=false -->

## Q1

**'task:macos-wkwebview-renderer-backend' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - Acceptance criterion 5 (the origin behaviour CONFIRMED at runtime on macOS, recorded, with the iOS caveat updated to what is now measured) is not delivered, and with it the runtime halves of criteria 1, 4 and 6: nothing in this diff has ever been compiled against a macOS SDK or executed. Can the human run the macos-14 job (it triggers on this PR) and then either re-stamp expected.json with the real OS/WebKit build or fix what the run names, before this lands? (expected.json first field: NOT YET MEASURED ... a falsifiable PREDICTION; README 'What still awaits a Mac': the macos-renderer job has not yet been run, every CI-proves item is a job that EXISTS; DIAGNOSIS addendum: the probe has been BUILT but not yet RUN. The Windows sibling landed the opposite way (measured expected.json + probe-report-2026-07-30.json + ADR-0011 Amendment 2). ~1500 lines of blind objc2 wiring are type-checked-from-Linux only, per DECISIONS.md 8.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q1 fields: id=q1 kind=stuck -->

**Your answer** (write below this line):

## Q2

**'task:macos-wkwebview-renderer-backend' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - Acceptance criterion 5 (the origin behaviour CONFIRMED at runtime on macOS, recorded, with the iOS caveat updated to what is now measured) is not delivered, and with it the runtime halves of criteria 1, 4 and 6: nothing in this diff has ever been compiled against a macOS SDK or executed. Can the human run the macos-14 job (it triggers on this PR) and then either re-stamp expected.json with the real OS/WebKit build or fix what the run names, before this lands? (expected.json first field: NOT YET MEASURED ... a falsifiable PREDICTION; README 'What still awaits a Mac': the macos-renderer job has not yet been run, every CI-proves item is a job that EXISTS; DIAGNOSIS addendum: the probe has been BUILT but not yet RUN. The Windows sibling landed the opposite way (measured expected.json + probe-report-2026-07-30.json + ADR-0011 Amendment 2). ~1500 lines of blind objc2 wiring are type-checked-from-Linux only, per DECISIONS.md 8.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q2 fields: id=q2 kind=stuck -->

**Your answer** (write below this line):

## Q3

**'task:macos-wkwebview-renderer-backend' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - Acceptance criterion 5 (the origin behaviour CONFIRMED at runtime on macOS, recorded, with the iOS caveat updated to what is now measured) is not delivered, and with it the runtime halves of criteria 1, 4 and 6: nothing in this diff has ever been compiled against a macOS SDK or executed. Can the human run the macos-14 job (it triggers on this PR) and then either re-stamp expected.json with the real OS/WebKit build or fix what the run names, before this lands? (expected.json first field: NOT YET MEASURED ... a falsifiable PREDICTION; README 'What still awaits a Mac': the macos-renderer job has not yet been run, every CI-proves item is a job that EXISTS; DIAGNOSIS addendum: the probe has been BUILT but not yet RUN. The Windows sibling landed the opposite way (measured expected.json + probe-report-2026-07-30.json + ADR-0011 Amendment 2). ~1500 lines of blind objc2 wiring are type-checked-from-Linux only, per DECISIONS.md 8.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q3 fields: id=q3 kind=stuck -->

**Your answer** (write below this line):

## Q4

**'task:macos-wkwebview-renderer-backend' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - Acceptance criterion 5 (the origin behaviour CONFIRMED at runtime on macOS, recorded, with the iOS caveat updated to what is now measured) is not delivered, and with it the runtime halves of criteria 1, 4 and 6: nothing in this diff has ever been compiled against a macOS SDK or executed. Can the human run the macos-14 job (it triggers on this PR) and then either re-stamp expected.json with the real OS/WebKit build or fix what the run names, before this lands? (expected.json first field: NOT YET MEASURED ... a falsifiable PREDICTION; README 'What still awaits a Mac': the macos-renderer job has not yet been run, every CI-proves item is a job that EXISTS; DIAGNOSIS addendum: the probe has been BUILT but not yet RUN. The Windows sibling landed the opposite way (measured expected.json + probe-report-2026-07-30.json + ADR-0011 Amendment 2). ~1500 lines of blind objc2 wiring are type-checked-from-Linux only, per DECISIONS.md 8.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q4 fields: id=q4 kind=stuck -->

**Your answer** (write below this line):

## Q5

**'task:macos-wkwebview-renderer-backend' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - Acceptance criterion 5 (the origin behaviour CONFIRMED at runtime on macOS, recorded, with the iOS caveat updated to what is now measured) is not delivered, and with it the runtime halves of criteria 1, 4 and 6: nothing in this diff has ever been compiled against a macOS SDK or executed. Can the human run the macos-14 job (it triggers on this PR) and then either re-stamp expected.json with the real OS/WebKit build or fix what the run names, before this lands? (expected.json first field: NOT YET MEASURED ... a falsifiable PREDICTION; README 'What still awaits a Mac': the macos-renderer job has not yet been run, every CI-proves item is a job that EXISTS; DIAGNOSIS addendum: the probe has been BUILT but not yet RUN. The Windows sibling landed the opposite way (measured expected.json + probe-report-2026-07-30.json + ADR-0011 Amendment 2). ~1500 lines of blind objc2 wiring are type-checked-from-Linux only, per DECISIONS.md 8.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q5 fields: id=q5 kind=stuck -->

**Your answer** (write below this line):
