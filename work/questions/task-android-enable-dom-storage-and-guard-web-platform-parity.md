<!-- dorfl-sidecar: item=task:android-enable-dom-storage-and-guard-web-platform-parity type=task slug=android-enable-dom-storage-and-guard-web-platform-parity allAnswered=false -->

## Q1

**'task:android-enable-dom-storage-and-guard-web-platform-parity' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The web-storage row marks macos, windows and ios as implemented on zero measurement, while the task's acceptance criterion said implemented where verified and stubbed with a real task slug where not. Should those three cells be stubbed against the already-authored slug matrix-web-platform-rows-are-measured-on-every-edge (which resolves, so the guard stays green), or does a human explicitly ratify Decision 1? (docs/platform-capability-matrix.toml web-storage row: cells all implemented; rationale is engine default plus no edge-side disabling. But the repo's own MEASURED note work/notes/observations/service-worker-registration-differs-by-ipfs-serving-origin-2026-07-30.md records that engine capabilities on custom-scheme origins are SCHEME-gated (serviceWorker.register rejects with InvalidStateError on a registered, secure ipfs:// tuple origin on Blink; the same for WKWebView), so no-toggle-exists does not establish that localStorage works on an ipfs:// origin in WKWebView/WebView2. ADR-0005 records the same over-claim being corrected once already (the seed listed implemented-everywhere where it was false). A machine-readable implemented on three unmeasured edges defuses the one row built to stop this class recurring.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q1 fields: id=q1 kind=stuck -->

**Your answer** (write below this line):

## Q2

**'task:android-enable-dom-storage-and-guard-web-platform-parity' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The web-storage row marks macos, windows and ios as implemented on zero measurement, while the task's acceptance criterion said implemented where verified and stubbed with a real task slug where not. Should those three cells be stubbed against the already-authored slug matrix-web-platform-rows-are-measured-on-every-edge (which resolves, so the guard stays green), or does a human explicitly ratify Decision 1? (docs/platform-capability-matrix.toml web-storage row: cells all implemented; rationale is engine default plus no edge-side disabling. But the repo's own MEASURED note work/notes/observations/service-worker-registration-differs-by-ipfs-serving-origin-2026-07-30.md records that engine capabilities on custom-scheme origins are SCHEME-gated (serviceWorker.register rejects with InvalidStateError on a registered, secure ipfs:// tuple origin on Blink; the same for WKWebView), so no-toggle-exists does not establish that localStorage works on an ipfs:// origin in WKWebView/WebView2. ADR-0005 records the same over-claim being corrected once already (the seed listed implemented-everywhere where it was false). A machine-readable implemented on three unmeasured edges defuses the one row built to stop this class recurring.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q2 fields: id=q2 kind=stuck -->

**Your answer** (write below this line):

## Q3

**'task:android-enable-dom-storage-and-guard-web-platform-parity' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The web-storage row marks macos, windows and ios as implemented on zero measurement, while the task's acceptance criterion said implemented where verified and stubbed with a real task slug where not. Should those three cells be stubbed against the already-authored slug matrix-web-platform-rows-are-measured-on-every-edge (which resolves, so the guard stays green), or does a human explicitly ratify Decision 1? (docs/platform-capability-matrix.toml web-storage row: cells all implemented; rationale is engine default plus no edge-side disabling. But the repo's own MEASURED note work/notes/observations/service-worker-registration-differs-by-ipfs-serving-origin-2026-07-30.md records that engine capabilities on custom-scheme origins are SCHEME-gated (serviceWorker.register rejects with InvalidStateError on a registered, secure ipfs:// tuple origin on Blink; the same for WKWebView), so no-toggle-exists does not establish that localStorage works on an ipfs:// origin in WKWebView/WebView2. ADR-0005 records the same over-claim being corrected once already (the seed listed implemented-everywhere where it was false). A machine-readable implemented on three unmeasured edges defuses the one row built to stop this class recurring.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q3 fields: id=q3 kind=stuck -->

**Your answer** (write below this line):

## Q4

**'task:android-enable-dom-storage-and-guard-web-platform-parity' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The web-storage row marks macos, windows and ios as implemented on zero measurement, while the task's acceptance criterion said implemented where verified and stubbed with a real task slug where not. Should those three cells be stubbed against the already-authored slug matrix-web-platform-rows-are-measured-on-every-edge (which resolves, so the guard stays green), or does a human explicitly ratify Decision 1? (docs/platform-capability-matrix.toml web-storage row: cells all implemented; rationale is engine default plus no edge-side disabling. But the repo's own MEASURED note work/notes/observations/service-worker-registration-differs-by-ipfs-serving-origin-2026-07-30.md records that engine capabilities on custom-scheme origins are SCHEME-gated (serviceWorker.register rejects with InvalidStateError on a registered, secure ipfs:// tuple origin on Blink; the same for WKWebView), so no-toggle-exists does not establish that localStorage works on an ipfs:// origin in WKWebView/WebView2. ADR-0005 records the same over-claim being corrected once already (the seed listed implemented-everywhere where it was false). A machine-readable implemented on three unmeasured edges defuses the one row built to stop this class recurring.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q4 fields: id=q4 kind=stuck -->

**Your answer** (write below this line):
