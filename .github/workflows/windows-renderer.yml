name: windows-renderer

# The Windows leg (task `windows-renderer-ci-leg`, sub-task 1 of
# `docs/adr/0011-webview2-for-windows.md`'s Windows split). It lands BEFORE any
# Windows shell code, on purpose and for a MECHANICAL reason:
#
#   `gh workflow run <wf> --ref <branch>` is only legal once `<wf>` exists on the
#   DEFAULT branch. A build agent therefore cannot dispatch a leg it is itself
#   inventing, which is exactly why both macOS tasks shipped a PREDICTION where
#   an acceptance criterion demanded a MEASUREMENT. With this file on `main`, the
#   two Windows code tasks (`windows-webview2-renderer-backend`,
#   `windows-win32-window-and-chrome`) can be dispatched against a real
#   `windows-latest` runner by ref, exactly as `macos-renderer.yml` made the
#   AppKit window provable (run 30572253620).
#
# WHAT IT PROVES TODAY. It builds and tests the repo's TOOLKIT-FREE half on
# `x86_64-pc-windows-msvc` — `webview-shared` leads that set, holding
# `LoadLifecycle`/`SharedLifecycle`, the `navigate` URL rule and the ADR-0008
# off-thread `ipfs://` boundary the WebView2 backend reuses VERBATIM — and, since
# task `windows-webview2-renderer-backend`, it also builds, tests and RUNS
# `crates/windows-renderer`: the `#[cfg(windows)]` WebView2 `Renderer` backend the
# Ubuntu gate can only PARSE. Since task `windows-win32-window-and-chrome` it
# also builds, tests and RUNS `crates/werust-windows` -- the Win32 WINDOW over
# that backend -- through `window_smoke`, which CONSTRUCTS the real window
# off-screen (toolbar, trust indicator and its EXPLANATION, error banner, ⋮ menu,
# tabbed debug view), loads a pinned hash-verified `ipfs://` fixture through the
# production verifying route and asserts what the real WIDGETS hold, then fails a
# tampered CID and asserts the banner takes the page. Since task
# `shortcuts-and-mouse-history-buttons-on-the-windows-edge` that same smoke also
# drives the CONVENTIONAL SHORTCUTS as real messages: a posted `WM_KEYDOWN` (F5)
# through the message-loop filter, a page-focused key through the engine's
# `AcceleratorKeyPressed` hook, and a posted `WM_XBUTTONDOWN` (`XBUTTON1`) that
# really navigates history. That is the ONLY place the
# Win32 layer is executed at all; everything derivable about it is unit-tested on
# the Ubuntu gate (`crates/desktop-paint`, the shared painter half both native
# desktop windows consume, and `crates/werust-windows/src/shortcuts.rs`, the pure
# Win32-to-`werust_core::shortcuts` translation). What a runner still cannot
# press is recorded at
# docs/spikes/shortcuts-and-mouse-history-buttons-on-the-windows-edge/README.md.
#
# The `trust_hooks_smoke` step is the one that
# matters for the ENGINE, because a backend qualifies for werust on the TRUST
# HOOKS, not on rendering: it loads a hash-verified `ipfs://<cid>` page through the production
# verifying resolver (pinned in memory — no gateway, no network), asserts the
# document got its REAL `ipfs://<cid>` tuple origin and sees the native EIP-1193
# `window.ethereum`, and then asserts a tampered CID FAILS the load and is never
# reported verified. The original crate set was determined by MEASUREMENT (a
# `cargo xwin check --target x86_64-pc-windows-msvc --tests` sweep over every
# workspace member), not by assumption; the method, the results and the named
# exclusions are recorded in `docs/spikes/windows-renderer-ci-leg/README.md`.
#
# EXCLUDED, with reasons: `werust` and `webview-renderer` bind gtk4/webkit6
# through pkg-config and are measured RED for this target (nothing on a Windows
# runner can satisfy those `.pc` files) — they are the Linux desktop's, and the
# Ubuntu `verify` gate builds them. `macos-renderer`, `werust-macos`,
# `macos-origin-probe`, `werust-android-core` and `werust-ios-core` are measured
# GREEN here only because their platform halves are `cfg`-gated away to nearly
# nothing on Windows, so building them here would assert nothing their own legs
# do not already assert. `native-renderer` and `script-engine` are measured GREEN
# for real, but sit outside the Windows-shell reuse path (they are the future
# native stack, fully covered by the Ubuntu gate) and carry the heaviest
# dependency trees in the workspace, so they are left out on COST, not on
# redness; a later task may add them.
#
# A SEPARATE leg from the pure-Rust Ubuntu `verify` gate, exactly like
# `mobile-ios.yml`, `macos-renderer.yml` and `windows-origin-probe.yml`. It is
# also distinct from `windows-origin-probe.yml`, which MEASURES WebView2 origin
# behaviour against a recorded verdict; this leg compiles and tests Rust.
#
# WHAT THE `pull_request` FILTER CARRIES. Everything that is genuinely
# Windows-shaped, plus the one shared half whose breakage this leg is the place
# to see: `crates/windows-renderer/**` (the WebView2 backend) and
# `crates/werust-windows/**` (the Win32 window), whose `cfg(windows)` halves
# compile NOWHERE else, so a PR touching either is exactly the case this leg
# exists to gate; `crates/webview-shared/**`, the toolkit-free half the backend
# reuses verbatim; `crates/windows-origin-probe/**`; `crates/desktop-paint/**`,
# the SHARED painter both native desktop windows consume, so a change to it
# changes what the window smoke below asserts; plus the shared version action and
# this file.
#
# That is WIDER than the filter this leg landed with, which carried only
# `webview-shared`, the probe, the action and this file. When it landed, the
# probe really was the only Windows-specific code in the tree. The engine
# arrived with `windows-webview2-renderer-backend`, the window and the shared
# painter with `windows-win32-window-and-chrome`, and each was added on purpose:
# gating a PR that touches Windows-only code on the only leg that compiles it is
# the discrimination the original narrow choice was making, not a departure from
# it. `crates/werust-core/tests/windows_renderer_leg_shape.rs` now pins the PR
# filter EXACTLY (no entry may join or leave it without editing that test), so
# the widening-by-accretion this comment used to trail behind cannot happen
# silently again.
#
# WHAT STAYS OFF THE PR FILTER, AND WHY (the deliberate trade-off, unchanged
# since this leg landed). The wider DEPENDENCY surface this leg builds
# (`werust-core`, `fetcher`, `renderer`) is caught on `push` to `main` instead:
# early detection WITHOUT gating every core PR on a cross-platform runner, plus
# `workflow_dispatch` for the deliberate case (which is this leg's whole purpose
# anyway). The cost of the trade is honest and bounded: a core change that breaks
# the Windows build is found minutes after it merges rather than before, on a leg
# that gates nothing. The same guard pins this half too, so narrowing or
# broadening it stays a decision rather than a reflex.
#
# The sibling `macos-renderer.yml` USED to differ here -- it triggered on PRs
# touching `crates/werust-core/**`, so most core work spent `macos-14` minutes and
# could be gated by a red cross-platform leg, the cost this leg refused to copy.
# Task `macos-harness-guard-teeth-and-paint-path-residue` settled that the other
# way: the macOS leg now keeps `werust-core` on `push` only and pins its own PR
# filter exactly (`crates/macos-renderer/tests/macos_backend_shape.rs`). Both
# desktop legs now answer the same question the same way, which is what makes the
# answer a rule rather than a preference.
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "crates/windows-renderer/**"
      - "crates/werust-windows/**"
      - "crates/desktop-paint/**"
      - "crates/webview-shared/**"
      - "crates/renderer/**"
      - "crates/werust-core/**"
      - "crates/fetcher/**"
      - "crates/windows-origin-probe/**"
      # The spike directory is KEPT here, deliberately (task
      # `windows-backend-error-mapping-and-leg-header-accuracy`), and only here:
      # it holds the RECORDED EVIDENCE: the verbatim `trust_hooks_smoke`
      # transcript this leg's claims are stamped from, and the local
      # cross-target type-check harness both Windows crates are iterated with.
      # Re-recording evidence is precisely the change worth re-measuring, which
      # is the same logic `macos-renderer.yml` applies to its spike paths. The
      # price is that a README typo in that directory burns one `windows-latest`
      # run on `main`, where this leg gates nothing. That is cheap enough that
      # keeping the evidence honest wins. It is deliberately NOT on the `pull_request`
      # filter below, so no docs edit is ever GATED on a Windows runner.
      - "docs/spikes/windows-webview2-renderer-backend/**"
      - ".github/actions/webview2-runtime-version/**"
      - ".github/workflows/windows-renderer.yml"
  # Pinned EXACTLY by `crates/werust-core/tests/windows_renderer_leg_shape.rs`:
  # no path can join or leave this LIST without that test going red, which is
  # what makes a widening a decision. The header paragraph above describes the
  # same list in prose, but no test holds PROSE — keeping the two in step is a
  # habit, not a guard, so update the header in the same change (task
  # `windows-release-packaging-leg` softened this comment, which used to claim
  # "neither can move without the other going red").
  pull_request:
    paths:
      - "crates/windows-renderer/**"
      - "crates/werust-windows/**"
      # The shared painter, KEPT here deliberately (task
      # `windows-backend-error-mapping-and-leg-header-accuracy`): it is not
      # Windows-shaped, but it is the one carrier both native desktop windows
      # paint from, so a break in it is genuinely cross-platform and the window
      # smoke below is what catches it. `macos-renderer.yml` carries it on its
      # PR filter for the same reason.
      - "crates/desktop-paint/**"
      - "crates/webview-shared/**"
      - "crates/windows-origin-probe/**"
      - ".github/actions/webview2-runtime-version/**"
      - ".github/workflows/windows-renderer.yml"

permissions:
  contents: read

jobs:
  windows-crates:
    runs-on: windows-latest
    steps:
      - name: Check the tree out byte-identically (no CRLF rewrite)
        # Git for Windows defaults to `core.autocrlf=true`, so a Windows checkout
        # would rewrite every LF to CRLF. Several tests in this repo PARSE
        # committed source text and match multi-line patterns
        # (`crates/werust-core/tests/*_shape.rs`), so a rewritten tree would fail
        # them for a reason that has nothing to do with Windows. Disabling the
        # conversion makes the runner test the repo's actual BYTES, identically
        # to the Ubuntu gate. Must run BEFORE the checkout.
        run: git config --global core.autocrlf false

      - name: Checkout
        uses: actions/checkout@v4

      - name: Record what this run measured on
        # The ONE registry read, shared with `windows-origin-probe.yml`. The
        # WebView2 Runtime is evergreen: a Windows result without its runtime
        # version is not a result, and this leg now DRIVES a real WebView2 in the
        # trust-hooks step below, so this is what dates that measurement.
        uses: ./.github/actions/webview2-runtime-version

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-windows-renderer-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-windows-renderer-cargo-

      - name: Build the Windows backend and the toolkit-free crates on x86_64-pc-windows-msvc
        # Package-SCOPED, never a bare workspace build: the workspace contains
        # GTK/WebKitGTK-bound crates that cannot build on a Windows runner.
        # `windows-renderer` and `windows-origin-probe` build their `cfg(windows)`
        # WebView2 halves here, which the Ubuntu gate can only parse.
        run: cargo build -p windows-renderer -p werust-windows -p desktop-paint -p webview-shared -p renderer -p werust-core -p fetcher -p windows-origin-probe

      - name: Test the Windows backend and the toolkit-free crates on x86_64-pc-windows-msvc
        # The backend's own tests, plus the half it reuses verbatim, exercised on
        # the platform that reuses it: `webview-shared`'s load lifecycle, navigate
        # URL rule and off-thread `ipfs://` boundary; `werust-core`'s chrome
        # derivation, ENS/IPNS and debug capture; `fetcher`'s hash-verify
        # boundary; and the origin probe's decision rules. All offline.
        run: cargo test -p windows-renderer -p werust-windows -p desktop-paint -p webview-shared -p renderer -p werust-core -p fetcher -p windows-origin-probe

      - name: Exercise BOTH trust hooks on a real WebView2 (with a negative control)
        # A backend qualifies on the trust hooks, not on rendering. This loads a
        # hash-verified `ipfs://<cid>` page through the production verifying
        # resolver (pinned in memory: no gateway, no network), asserts the
        # document got its REAL `ipfs://<cid>` tuple origin — ADR-0011 Amendment
        # 2's verdict, observed from inside the BACKEND rather than the probe —
        # asserts the page sees the native EIP-1193 provider and round-trips a
        # request, then asserts a tampered CID FAILS the load and is never
        # reported verified. A smoke where everything passes has measured nothing.
        run: cargo run -p windows-renderer --example trust_hooks_smoke

      - name: Build and drive the REAL Win32 window (chrome, menu, debug view)
        # The window half's only execution anywhere: it opens a real top-level
        # window FAR off-screen (never activated, so nothing is shown and no
        # focus is stolen), loads a pinned, in-memory hash-verified `ipfs://` page
        # through the production verifying route, and asserts what the real
        # widgets hold -- the URL bar, the trust indicator AND its explanation
        # (read back off the real tooltip), the status line, the ⋮ menu built from
        # the core's `BrowserMenu`, the debug view catching the page's own
        # `console.log`, and the DURABLE `%LOCALAPPDATA%` profile really existing
        # on disk. Then a negative control (bytes that do not hash to their CID)
        # must FAIL, raise the error banner and displace the page. Offline and
        # deterministic.
        #
        # Since task `windows-release-packaging-leg` this example also LINKS the
        # application manifest the shipped binary carries (that crate's build
        # script applies it to bins AND examples on purpose), so the window is
        # driven under comctl32 v6 rather than 5.82 -- i.e. under the same
        # configuration a user gets. This step is therefore where a v6 behaviour
        # difference surfaces, which is exactly where it should.
        run: cargo run -p werust-windows --example window_smoke
