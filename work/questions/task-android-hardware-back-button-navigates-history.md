<!-- dorfl-sidecar: item=task:android-hardware-back-button-navigates-history type=task slug=android-hardware-back-button-navigates-history allAnswered=false -->

## Q1

**'task:android-hardware-back-button-navigates-history' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The source-shape guard for acceptance criterion 3 (system Back uses the SAME off-UI-thread driveCore path) is VACUOUS: it passes even when handleOnBackPressed is completely empty. The helper kotlin_fun_body picks its terminator by KIND, not by POSITION - rest.find(newline+4spaces+'private fun ') is tried FIRST and returns Some (matching the much later 'private fun driveCore'), so or_else never considers the nearer 'override fun ' terminator. The extracted 'handler' body is ~9700 chars and swallows all of onCreate, so the positive assert is satisfied by the ON-SCREEN button line 'compactNavButton(back-glyph) { driveCore { core.goBack() } }' inside onCreate, not by the handler. I confirmed by mutation: replacing the handler body with an empty block keeps all four asserts green. Fix by bounding the body at the MINIMUM index over the terminator candidates (or by extracting the systemBackCallback object literal instead). (crates/werust-android/rust/tests/system_back_wiring_shape.rs kotlin_fun_body + the_system_back_drives_the_core_off_the_ui_thread_like_the_on_screen_button. The sibling refreshChrome extraction is fine (its next member IS a private fun), so the lockstep test is genuinely valid; only the handler test is hollow.)
> - Three artifacts CLAIM the guard pins a property it does not pin, so the claim is drift, not just a weak test: the matrix row says the wiring 'carries the strongest automatable guard', the spike README says the shape half asserts 'handleOnBackPressed drives driveCore { core.goBack() } (never an inline UI-thread core call)', and the test module doc maps criterion 3 to that assertion. Until the extractor is fixed, a future refactor that calls the core inline on the UI thread (re-opening the ANR the previous task fixed) ships with a green gate and docs that say it cannot. (docs/platform-capability-matrix.toml capability system-back-navigates-history; docs/spikes/android-hardware-back-button-navigates-history/README.md 'What is automatable vs runtime-only'; system_back_wiring_shape.rs module doc criterion 3. Note the Gradle/Kotlin build is NOT in verify (cargo-only), so this shape test is the only gate-side cover for the edge wiring.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q1 fields: id=q1 kind=stuck -->

**Your answer** (write below this line):

## Q2

**'task:android-hardware-back-button-navigates-history' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The source-shape guard for acceptance criterion 3 (system Back uses the SAME off-UI-thread driveCore path) is VACUOUS: it passes even when handleOnBackPressed is completely empty. The helper kotlin_fun_body picks its terminator by KIND, not by POSITION - rest.find(newline+4spaces+'private fun ') is tried FIRST and returns Some (matching the much later 'private fun driveCore'), so or_else never considers the nearer 'override fun ' terminator. The extracted 'handler' body is ~9700 chars and swallows all of onCreate, so the positive assert is satisfied by the ON-SCREEN button line 'compactNavButton(back-glyph) { driveCore { core.goBack() } }' inside onCreate, not by the handler. I confirmed by mutation: replacing the handler body with an empty block keeps all four asserts green. Fix by bounding the body at the MINIMUM index over the terminator candidates (or by extracting the systemBackCallback object literal instead). (crates/werust-android/rust/tests/system_back_wiring_shape.rs kotlin_fun_body + the_system_back_drives_the_core_off_the_ui_thread_like_the_on_screen_button. The sibling refreshChrome extraction is fine (its next member IS a private fun), so the lockstep test is genuinely valid; only the handler test is hollow.)
> - Three artifacts CLAIM the guard pins a property it does not pin, so the claim is drift, not just a weak test: the matrix row says the wiring 'carries the strongest automatable guard', the spike README says the shape half asserts 'handleOnBackPressed drives driveCore { core.goBack() } (never an inline UI-thread core call)', and the test module doc maps criterion 3 to that assertion. Until the extractor is fixed, a future refactor that calls the core inline on the UI thread (re-opening the ANR the previous task fixed) ships with a green gate and docs that say it cannot. (docs/platform-capability-matrix.toml capability system-back-navigates-history; docs/spikes/android-hardware-back-button-navigates-history/README.md 'What is automatable vs runtime-only'; system_back_wiring_shape.rs module doc criterion 3. Note the Gradle/Kotlin build is NOT in verify (cargo-only), so this shape test is the only gate-side cover for the edge wiring.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q2 fields: id=q2 kind=stuck -->

**Your answer** (write below this line):

## Q3

**'task:android-hardware-back-button-navigates-history' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The source-shape guard for acceptance criterion 3 (system Back uses the SAME off-UI-thread driveCore path) is VACUOUS: it passes even when handleOnBackPressed is completely empty. The helper kotlin_fun_body picks its terminator by KIND, not by POSITION - rest.find(newline+4spaces+'private fun ') is tried FIRST and returns Some (matching the much later 'private fun driveCore'), so or_else never considers the nearer 'override fun ' terminator. The extracted 'handler' body is ~9700 chars and swallows all of onCreate, so the positive assert is satisfied by the ON-SCREEN button line 'compactNavButton(back-glyph) { driveCore { core.goBack() } }' inside onCreate, not by the handler. I confirmed by mutation: replacing the handler body with an empty block keeps all four asserts green. Fix by bounding the body at the MINIMUM index over the terminator candidates (or by extracting the systemBackCallback object literal instead). (crates/werust-android/rust/tests/system_back_wiring_shape.rs kotlin_fun_body + the_system_back_drives_the_core_off_the_ui_thread_like_the_on_screen_button. The sibling refreshChrome extraction is fine (its next member IS a private fun), so the lockstep test is genuinely valid; only the handler test is hollow.)
> - Three artifacts CLAIM the guard pins a property it does not pin, so the claim is drift, not just a weak test: the matrix row says the wiring 'carries the strongest automatable guard', the spike README says the shape half asserts 'handleOnBackPressed drives driveCore { core.goBack() } (never an inline UI-thread core call)', and the test module doc maps criterion 3 to that assertion. Until the extractor is fixed, a future refactor that calls the core inline on the UI thread (re-opening the ANR the previous task fixed) ships with a green gate and docs that say it cannot. (docs/platform-capability-matrix.toml capability system-back-navigates-history; docs/spikes/android-hardware-back-button-navigates-history/README.md 'What is automatable vs runtime-only'; system_back_wiring_shape.rs module doc criterion 3. Note the Gradle/Kotlin build is NOT in verify (cargo-only), so this shape test is the only gate-side cover for the edge wiring.)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q3 fields: id=q3 kind=stuck -->

**Your answer** (write below this line):
