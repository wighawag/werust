## Changelog
### Features
* a3ef728 feat(android-apk-signing): Sign the Android APK in CI (release keystore → signed APK attached to the GitHub Release); done
* eb52d0e feat(android-apk-version-from-the-release-tag): Derive the APK versionCode/versionName from the release tag so a signed release can actually be sequenced as an update; done
* f5c8016 feat(android-release-tag-that-is-not-a-triple-must-not-ship-versioncode-1): A release tag that is not a clean triple must FAIL the APK build, not silently ship `versionCode = 1` again; done
* 75dbf83 feat(cli-resolve-follows-mutable-names-to-the-cid): `werust resolve` follows a mutable name through to the content CID (no flag), and speaks the core's protocol vocabulary instead of its own; done
* e8342b8 feat(desktop-chrome-presentation-into-core): Move the desktop chrome PRESENTATION rules out of the GTK edge into the shared toolkit-free core, so an edge only paints; done
* 69d7e82 feat(export-the-chrome-css-class-set-from-core): Export the chrome's CSS-class SET from core so a painter cannot keep a stale class list (and no exported class can go unstyled); done
* fa17b08 feat(export-the-chrome-css-class-set-from-core): Export the chrome's CSS-class SET from core so a painter cannot keep a stale class list (and no exported class can go unstyled); done
* c0a1a65 feat(headless-cli-mode): Desktop binary usable as a CLI (subcommands like `werust resolve ronan.eth`, `werust fetch ipfs://...`) without a GUI; done
* e772025 feat(ipns-tofu-pin-and-warn-on-change): TOFU for mutable names: let the user bless a name's current CID, then warn when it changes (SSH-host-key-style pin-and-warn); done
* 8d14c38 feat(macos-appkit-window-and-chrome): macOS: the AppKit window that PAINTS the chrome over the WKWebView backend; done
* 32ce022 feat(macos-appkit-window-and-chrome): macOS: the AppKit window that PAINTS the chrome over the WKWebView backend; done
* 2d8a72c feat(macos-harness-guard-teeth-and-paint-path-residue): Give the type-check harness guard teeth on BOTH branches, and sweep the docs that still point at the deleted `paint.rs`; done
* ed3eedc feat(macos-parity-column-and-stub-tasks): Add the `macos` column to the platform-capability parity matrix, and cut the stub tasks the guard then forces; done
* edd4202 feat(macos-release-packaging-leg): Package the macOS desktop build in CI: universal binary, unsigned `.app`, attached to the tagged Release; done
* b55a9d3 feat(macos-spike-doc-accuracy-and-harness-guard): Tidy the macOS engine's committed docs and put a guard on the type-check harness's `rm -rf`; done
* 0dd0b21 feat(macos-spike-doc-accuracy-and-harness-guard): Tidy the macOS engine's committed docs and put a guard on the type-check harness's `rm -rf`; done
* 733bc3e feat(macos-wkwebview-renderer-backend): macOS: the WKWebView `Renderer` backend (engine only, no window chrome); done
* c100636 feat(macos-wkwebview-renderer-backend): macOS: the WKWebView `Renderer` backend (engine only, no window chrome); done
* da2b081 feat(mobile-chrome-presentation-from-one-derivation): Collapse the Kotlin and Swift chrome-presentation twins onto the ONE shared derivation (and close the trust-explanation gap they revealed); done
* f7d6e5c feat(mobile-trust-badge-accessibility-announces-the-state-not-only-the-essay): A screen-reader user should still hear WHICH trust state the badge is in, not only the 240-character explanation of it; done
* fed6725 feat(one-derivation-close-the-aggregate-and-tooltip-gaps): Make the class-coverage gates exhaustive over FAMILIES by construction, and unify the URL-bar progress tooltip into core; done
* 64eb23b feat(pin-store-read-modify-write-and-test-isolation): A TOFU store that silently drops a pin is worse than no store: make blessing read-modify-write, and stop core tests reading the developer's real pins; done
* cf93e55 feat(pin-the-rust-toolchain-and-fix-the-197-clippy-red-on-main): URGENT: main is RED — pin the Rust toolchain so the gate is reproducible, and fix the 1.97 clippy lint the unpinned CI found; done
* 1d678c5 feat(verify-lints-test-targets-and-clears-the-existing-debt): Make `verify`'s clippy lint TEST targets too, clearing the existing debt in the same change so the gate is never knowingly left red; done
* 73b19a0 feat(verify-lints-test-targets-and-clears-the-existing-debt): Make `verify`'s clippy lint TEST targets too, clearing the existing debt in the same change so the gate is never knowingly left red; done
* f62a060 feat(versioned-gtk-app-id-and-stale-process-detection): Version the GTK application_id so a new werust binary opens its own window with its own compiled code instead of silently reusing an old process — the old binary may have stale behavior (wrong RPC endpoint, old compile-time constants); done
* 18dae30 feat(windows-app-manifest-is-malformed-xml-and-breaks-the-release-link): URGENT: the Windows release job fails to link — `app.manifest` uses `--` inside an XML comment, which XML forbids; done
* 1ba917f feat(windows-backend-error-mapping-and-leg-header-accuracy): Stop telling every WebView2 failure to install the runtime, and make the Windows leg's header describe the filter it actually has; done
* cda7616 feat(windows-chrome-must-scale-with-the-display-dpi): The Win32 chrome draws at 96 DPI on a high-DPI display, so it is half-size: scale it, now that the manifest promises Windows we do; done
* d9deec7 feat(windows-gui-subsystem-no-console-window): werust on Windows must not open a console window beside itself; done
* 8a3afe4 feat(windows-ipfs-origin-probe-on-ci): Windows origin probe (gate 0): does a registered `ipfs://` scheme in WebView2 give a real same-origin `fetch` + `pushState`, or must Windows use the internal-https origin map?; done
* 49b7e2e feat(windows-parity-column-and-stub-tasks): Add the `windows` column to the platform-capability parity matrix, and cut the stub tasks the guard then forces; done
* d225a2b feat(windows-platform-research): Research: what webview/UI backend for Windows (and what would it take to make werust cross-platform — GTK, webview2, or the native Rust renderer?); done
* 280dd50 feat(windows-probe-evidence-files-agree-test): Assert the Windows probe's two committed evidence files against each other in the ordinary gate, so the pinned verdict and the verbatim run cannot drift apart; done
* 3d04362 feat(windows-release-packaging-leg): Package the Windows desktop build in CI: an unsigned zip attached to the tagged Release, with the app manifest the chrome is waiting for; done
* ea232c9 feat(windows-renderer-ci-leg): Land the `windows-renderer` CI leg FIRST, so the Windows shell can be measured on a real runner instead of predicted; done
* d4692ff feat(windows-renderer-ci-leg): Land the `windows-renderer` CI leg FIRST, so the Windows shell can be measured on a real runner instead of predicted; done
* 5fff7fc feat(windows-webview2-renderer-backend): Windows: the WebView2 `Renderer` backend (engine only, no window chrome); done
* 7b0ffe0 feat(windows-win32-window-and-chrome): Windows: the Win32 window that PAINTS the chrome over the WebView2 backend; done
### Bug fixes
* 9ec9e83 fix(backlog): rewrite versioned-gtk-app-id task to foreground the real problem (stale compiled behavior serving the new session) rather than the cosmetic version label — the wrong RPC endpoint was the actual trap
* 5ed6fb6 fix(chrome): show load progress in the URL bar instead of a displacing banner
